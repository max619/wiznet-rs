{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargo build example: spi_dma_loopback",
            "type": "shell",
            "command": "cargo",
            "args": ["build", "--example", "spi_dma_loopback"],
            "problemMatcher": ["$rustc"],
            "group": "build"
        }
    ]
}
